// 运算符的计算顺序：先乘除、后加减
// 短路求值：当一个运算结果无法确定逻辑运算的结果时，才对第二个运算数进行求值。
// 短路求值，算是C语言的特性之一，并非所有的语言都遵循短路求值原则。
#include <stdio.h>
int main(){
    int a = 3;
    int b = 5;

    (a = 0) && (b = 5);
    printf("a = %d, b = %d\n",a,b);

    (a = 1) || (b = 5);
    printf("a = %d, b = %d\n",a,b);

    return 0;
}